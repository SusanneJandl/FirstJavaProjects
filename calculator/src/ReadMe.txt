positive numbers INCLUDING ZERO allowed
space after colon, before console input and between input and operator because it looks better
2 decimal places for input values and 3 decimal places for result visible in console output for average precision and good readability.
   for adaption of the decimal places go to Calculator (parent)class, method printOutput
